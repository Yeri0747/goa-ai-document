# Local Mongodb database (local or Dockerized) -------------
spring:
  data:
    mongodb:
      uri: mongodb://mongo:mongo@localhost:27017/goaaidocumentdb?authSource=admin

aws:
  s3:
    bucket: goa-ai-documents
